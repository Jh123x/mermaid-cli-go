# Mermaid CLI Go

This is a `Golang` CLI that converts a `mermaid` file into a `svg`

## How to use

```bash
go run main.go -i <path to mermaid file>
```

## Future Goals

- [ ] Markdown Support
- [ ] Other Feature Flags